import { Provider } from 'react-redux';
import { MemoryRouter as Router } from 'react-router-dom';

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { object, text } from "@storybook/addon-knobs";

import Store from 'core/store/store';

import Todo from "views/template/todo"

<Meta title="template" />

a template for todo page.  

<Preview>
  
  <Story name="todo">
    <Provider store={Store}>
      <Router>
        <Todo {...{
          headerProps: {
            userName: text('userNameForHeader', 'test user')
          },
          taskFormProps: {
            userName: text('userNameForTaskForm', 'test user')
          },
          taskListProps: {
            tasks: object("tasks", [
              { id: 1, title: 'task1', detail: 'detail1', assignee: 'assignee1'},
              { id: 2, title: 'task-two', detail: 'detail-two', assignee: 'assignee-two'}
            ])
          }
        }} />
      </Router>
    </Provider>
  </Story>
</Preview>

<Props of={Todo} />
